# Test Queries for Qdrant Search Algorithm Comparison

The following 5 natural-language queries represent distinct thematic categories within the 20 Newsgroups dataset:

1. **Query 1 (Graphics / Hardware)**:
   `What are the best GPU driver settings for 3D graphics rendering performance?`
   - *Target Domain*: `comp.graphics` / `comp.sys.ibm.pc.hardware`

2. **Query 2 (Cryptography / Security)**:
   `How does public-key cryptography and RSA encryption keep web data secure?`
   - *Target Domain*: `sci.crypt`

3. **Query 3 (Space / Astronomy)**:
   `What are the latest discoveries about planetary orbits and NASA space shuttle launches?`
   - *Target Domain*: `sci.space`

4. **Query 4 (Motorcycles / Mechanics)**:
   `What is the standard maintenance procedure for adjusting motorcycle clutch cables?`
   - *Target Domain*: `rec.motorcycles`

5. **Query 5 (Medicine / Health)**:
   `How do medical researchers diagnose autoimmune diseases and treat viral infections?`
   - *Target Domain*: `sci.med`
